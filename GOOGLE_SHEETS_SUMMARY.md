# Интеграция Google Sheets - Итоговая сводка

## ✅ Реализовано

### 1. Установка зависимостей

- ✅ Добавлены `gspread==6.0.0` и `google-auth==2.26.2` в `requirements.txt`

### 2. Конфигурация

- ✅ Расширен `config.py` тремя новыми параметрами:
  - `google_sheets_enabled` (по умолчанию: true)
  - `google_sheets_credentials_file` (путь к JSON)
  - `google_sheets_spreadsheet_id` (ID таблицы)
- ✅ Создан `.env.example` с примерами переменных окружения

### 3. Модуль Google Sheets (`backend/google_sheets.py`)

- ✅ Асинхронная функция `append_registration_to_sheet()`
- ✅ Fail-safe режим (ошибки не прерывают регистрацию)
- ✅ Автоматическое создание заголовков в таблице
- ✅ Функция тестирования подключения `test_google_sheets_connection()`
- ✅ Кэширование клиента Google Sheets
- ✅ Выполнение в executor для неблокирующих операций

### 4. Интеграция в API

- ✅ Импорт модуля в `fastapi_start_loft.py`
- ✅ Вызов `append_registration_to_sheet()` после сохранения в MongoDB
- ✅ Обработка ошибок с логированием (try-except)
- ✅ Передача данных регистрации и названия турнира

### 5. Скрипт миграции (`backend/migrate_to_sheets.py`)

- ✅ Миграция существующих данных из MongoDB в Sheets
- ✅ Поддержка `--dry-run` для предпросмотра
- ✅ Поддержка `--limit N` для ограничения количества
- ✅ Прогресс-бар и статистика
- ✅ Автоматическое получение названий турниров
- ✅ Rate limiting для соблюдения квот API

### 6. Документация

- ✅ **GOOGLE_SHEETS_SETUP.md** - подробная инструкция настройки
- ✅ **GOOGLE_SHEETS_QUICKSTART.md** - быстрый старт
- ✅ **TODO_GOOGLE_SHEETS.md** - памятка для вас
- ✅ Обновлён **backend/README.md** с информацией об интеграции

### 7. Безопасность

- ✅ Файлы credentials уже защищены в `.gitignore`
- ✅ Fail-safe режим (сбой Sheets не ломает регистрацию)
- ✅ Логирование всех ошибок

## 📊 Структура данных в Google Sheets

Автоматически создаются заголовки:

| Колонка          | Данные                         |
| ---------------- | ------------------------------ |
| ID               | MongoDB ObjectId регистрации   |
| Турнир ID        | ID турнира                     |
| Название турнира | Название турнира для удобства  |
| ФИО              | Полное имя участника           |
| Телефон          | Телефон в формате +7XXXXXXXXXX |
| Категория        | Категория участника            |
| Звание           | Спортивное звание              |
| Город/Страна     | Город и страна                 |
| Дата рождения    | Опционально                    |
| Комментарий      | Опционально                    |
| Статус           | new/confirmed/cancelled        |
| Дата создания    | Timestamp регистрации          |
| IP               | IP адрес клиента               |
| User Agent       | Браузер пользователя           |

## 🚀 Что нужно сделать для запуска

### Локально:

1. Создать Service Account в Google Cloud Console
2. Скачать JSON credentials
3. Создать Google Sheets таблицу
4. Предоставить доступ service account к таблице
5. Настроить `.env` (3 переменные)
6. `pip install -r requirements.txt`
7. `python migrate_to_sheets.py` (мигрировать старые данные)
8. `python fastapi_start_loft.py` (запустить)

### На production:

1. Загрузить JSON credentials на сервер
2. Добавить переменные в `.env`
3. `pip install -r requirements.txt`
4. `python migrate_to_sheets.py`
5. `pm2 restart startloft-backend`

## 📁 Созданные файлы

```
backend/
├── google_sheets.py              # Модуль интеграции
├── migrate_to_sheets.py          # Скрипт миграции
├── .env.example                  # Пример конфигурации
├── config.py                     # Обновлён
├── fastapi_start_loft.py         # Обновлён
├── requirements.txt              # Обновлён
└── README.md                     # Обновлён

/
├── GOOGLE_SHEETS_SETUP.md        # Подробная инструкция
├── GOOGLE_SHEETS_QUICKSTART.md   # Быстрый старт
└── TODO_GOOGLE_SHEETS.md         # Памятка для вас
```

## 🔧 Основные функции

### `append_registration_to_sheet(registration_data, tournament_name)`

Асинхронно добавляет регистрацию в Google Sheets таблицу.

**Параметры:**

- `registration_data` - словарь с данными регистрации (из MongoDB)
- `tournament_name` - название турнира (опционально)

**Возвращает:**

- `True` если успешно
- `False` если произошла ошибка (ошибка логируется)

### `test_google_sheets_connection()`

Тестирует подключение к Google Sheets.

**Возвращает:**

- `True` если подключение успешно
- `False` если ошибка

## ⚙️ Переменные окружения

```env
# Включить/выключить интеграцию
GOOGLE_SHEETS_ENABLED=true

# Путь к JSON credentials (относительно backend/)
GOOGLE_SHEETS_CREDENTIALS_FILE=start-loft-cb70bbfaa5b7.json

# ID таблицы из URL
GOOGLE_SHEETS_SPREADSHEET_ID=1a2b3c4d5e6f7g8h9i0j
```

## 🎯 Поведение

### При успешной регистрации:

1. Валидация данных
2. Сохранение в MongoDB ✅
3. Попытка сохранить в Google Sheets
   - Успешно → логируется
   - Ошибка → логируется, но регистрация успешна
4. Возврат успешного ответа клиенту

### При отключенной интеграции:

- `GOOGLE_SHEETS_ENABLED=false` → функция сразу возвращает False
- Регистрация работает как обычно (только MongoDB)

### При ошибках Google Sheets:

- Credentials не найдены → логируется, клиент None
- Spreadsheet не найден → логируется, False
- API error → логируется, False
- Регистрация ВСЕГДА успешна если сохранилась в MongoDB

## 📝 Логирование

Все операции логируются:

- `logger.info()` - успешные операции
- `logger.warning()` - отсутствие конфигурации
- `logger.error()` - ошибки API

Пример:

```
INFO: Google Sheets client initialized successfully
INFO: Successfully added registration to Google Sheets: Иванов Иван for tournament Чемпионат Казахстана
ERROR: Google Sheets API error: ...
```

## 🧪 Тестирование

```bash
# Тест подключения
python -c "import asyncio; from google_sheets import test_google_sheets_connection; asyncio.run(test_google_sheets_connection())"

# Dry run миграции
python migrate_to_sheets.py --dry-run

# Миграция 10 записей для теста
python migrate_to_sheets.py --limit 10

# Полная миграция
python migrate_to_sheets.py
```

## 💡 Возможные улучшения (опционально)

- [ ] Retry механизм для повторных попыток при сбоях API
- [ ] Batch операции для массовой записи (при миграции)
- [ ] Webhook для обратной синхронизации (Sheets → MongoDB)
- [ ] Форматирование ячеек (цвета для статусов)
- [ ] Автоматические фильтры в таблице
- [ ] Отдельные листы для каждого турнира
- [ ] Экспорт в Excel через Google Sheets API

## ✅ Готово к использованию!

Вся инфраструктура готова. Следуйте инструкции в `TODO_GOOGLE_SHEETS.md` для настройки.
